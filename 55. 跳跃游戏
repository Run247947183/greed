# greed
贪心算法
class Solution {
    // 贪心算法
    // 只要保证当前下标 i 小于等于 steps 能够跳跃到的地方
    // 那么就可以一直寻找最大的跳跃长度，就能判断是否能跳跃到最后一个位置
    public boolean canJump(int[] nums) {
        int len = nums.length;
        int steps = 0;
        for (int i = 0; i < len; i++) {
            if (i <= steps) {
                steps = Math.max(steps, i + nums[i]);
                if (steps >= len - 1) {
                    return true;
                }
            }
        }
        return false;
    }
}
